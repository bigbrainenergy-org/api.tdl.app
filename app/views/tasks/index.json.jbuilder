json.array! @tasks, :id, :list_id, :status_id, :schedule_id, :delegated,
  :status_last_changed_at, :deadline_at, :title, :notes, :order, :completed,
  :remind_me_at, :mental_energy_required, :physical_energy_required,
  :task_duration_in_minutes, :procedure_ids
